{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "yarn --cwd packages/web dev",
    "build": "yarn --cwd packages/web build",
    "start": "yarn --cwd packages/web start"
  }
}
